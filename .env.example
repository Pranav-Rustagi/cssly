# Web3Forms access key for the contact form (app/contact).
# Get one free at https://web3forms.com/ — sign up, create a form, copy the
# access key it gives you. Must be prefixed NEXT_PUBLIC_ since it's read in
# a client component and posted directly to Web3Forms with no API route.
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=
